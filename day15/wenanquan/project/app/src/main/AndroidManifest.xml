<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".base.App"
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mvp.ui.activity.tongpao.TongPaoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".mvp.ui.activity.SelCourseActivity" />
        <activity android:name=".mvp.ui.activity.Choose_courseActivity" />
        <activity android:name=".mvp.ui.activity.GuideActivity" />
        <activity
            android:name=".mvp.ui.activity.SplashActivity"
            android:theme="@style/AppSplashTheme">

            <!-- android:screenOrientation="portrait" -->
            <!-- 设置屏幕方向为竖屏 不管怎么横竖屏切换 屏幕方向一直为竖向 -->

        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".HomeActivity2" />
    </application>

</manifest>